<style>
    main {
        background: var(--dash-bg);
        background-attachment: fixed;
        color: var(--dash-fg);
        padding: var(--grid-padding);
    }
</style>

<main id="top">
    <slot/>
</main>
---
/** MDX prose inline `<code>` (smaller size in flowing text). */
import Typography from '@components/Typography.astro';
---
<Typography tagName="code" prose {...Astro.props}><slot /></Typography>

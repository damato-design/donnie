<style is:global>
    .rugged {
        filter: url(#rugged) drop-shadow(2px 0 4px rgba(20,20,20, .2));
        background: linear-gradient(9deg, #f0f0f0, #fdfdfd);
    }
</style>

<svg xmlns="http://www.w3.org/2000/svg" version="1.1" height="0" width="0">
    <defs>
        <filter id="rugged">
            <feTurbulence type="fractalNoise" baseFrequency=".1" numOctaves="4" />
            <feDisplacementMap in="SourceGraphic" scale="4" />
            <feComposite in="SourceGraphic" operator="in" />
        </filter>
    </defs>
</svg>

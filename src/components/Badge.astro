
<style>
    span {
        padding: .5em;
        display: inline-flex;
        align-items: center;
        align-self: center;
        line-height: 1;
        corner-shape: scoop;
        border-radius: 6px;
        gap: .5ch;
        border: 1px solid;
        background-image: var(--shine);
        color: color-mix(in oklch, var(--gold), black 70%);
        text-transform: uppercase;
        letter-spacing: 5px;
        font-size: .8em;
        font-weight: 800;

        &::before, &::after {
            content: '✹';
        }
    }
</style>

<span>
    <slot/>
</span>